rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 只要是登入的使用者就能讀寫（包含匿名登入）；
    // 因為玩家端用「匿名登入」自動完成，所以對玩家來說完全不需要輸入帳密。
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
